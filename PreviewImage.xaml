<Page
    x:Class="Media_App.PreviewImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Media_App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!--<Grid>
        <StackPanel>
            <Button Content="Button" Click="Button_Click"/>
            <Image x:Name="MyImage" PointerEntered="preview_hover" PointerExited="preview_out"/>
        </StackPanel> 
    </Grid>
    -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="grid_scroll">
            <VariableSizedWrapGrid x:Name="TheGrid" Orientation="Horizontal" ItemHeight="180" ItemWidth="320" SizeChanged="grid_sizeChanged" />
        </ScrollViewer>
        <!--<Popup/>-->
        <MediaPlayerElement x:Name="ThePlayer" AreTransportControlsEnabled="True" IsFullWindow="True" KeyDown="player_keydown" DoubleTapped="player_doubletapped"/>
        
    </Grid>
</Page>
